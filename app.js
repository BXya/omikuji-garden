// omikuji-garden entry module.
// Real logic lands in later slices (signbox, draw, overlay).

function init() {
  // Smoke check: prove ES module boot works. Remove in slice 2.
  console.log("[omikuji-garden] init ok");
}

init();
